package com.nirbhor.flutter

import io.flutter.embedding.android.FlutterActivity

/**
 * The single Flutter activity. It carries `showWhenLocked` and `turnScreenOn` in the manifest so a
 * reminder's full-screen intent can put the alarm over the lock screen and wake the display.
 */
class MainActivity : FlutterActivity()
